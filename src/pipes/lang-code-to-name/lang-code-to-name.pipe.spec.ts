import {LangCodeToNamePipe} from './lang-code-to-name.pipe';

describe('LangCodeToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new LangCodeToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
