import { CkEditorDirective } from './ck-editor.directive';

describe('CkEditorDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new CkEditorDirective();
  //   expect(directive).toBeTruthy();
  // });
});
